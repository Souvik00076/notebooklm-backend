# RAG Exceptions

All RAG-related exceptions are defined in `app/rag/RagException.py`.

## Exceptions

| Exception | Description |
|-----------|-------------|
| `LoaderNotFoundException` | Raised when no loader is found for the given document type |

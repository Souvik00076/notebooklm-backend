# FastAPI and Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.25
alembic>=1.13.1
asyncpg>=0.29.0  # Async PostgreSQL driver
psycopg2-binary>=2.9.9  # Sync PostgreSQL driver (for Alembic migrations)

# Validation
pydantic>=2.5.3
pydantic-settings>=2.1.0
email-validator>=2.1.0

# Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-dotenv>=1.0.0

# CORS is built into FastAPI, no separate package needed

# Testing (optional, for future)
pytest>=7.4.4
httpx>=0.26.0

# LangChain
langchain-core
langchain-community
pypdf>=3.17.0  # Required for PDF document loading
python-docx>=1.1.0  # Required for DOCX document loading
docx2txt>=0.8  # Required for DOCX text extraction
unstructured>=0.12.0  # Required for DOC file loading
unstructured[docx]>=0.12.0  # Extra dependencies for Word documents
